server:
  port: 8083

spring:
  datasource:
    url: jdbc:postgresql://cart-db:5432/cartdb
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
