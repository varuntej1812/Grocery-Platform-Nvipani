server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://product-db:5432/productdb
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
